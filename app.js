const countDate = () => {
    const countDate = new Date("October 17, 2022 00:00:00"); //inserted is the date we are counting down to, this can be changed 
    console.log(countDate);
};

countDate();
